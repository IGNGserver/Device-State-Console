# UI before evidence

采集时间：2026-08-05（Asia/Shanghai）

来源：已安装的 `C:\Program Files\DeviceStateConsoleAgent\观澜.exe`，未改动项目源码。

当前状态：`DEVICE CONSOLE v0.2.68 (test)`；Hub snapshot 为 cached，local collector 为 stopped，设备数量为 0。

`baseline-window-full.png` 是当前窗口完整抓取；`baseline-window.png` 是窄窗口裁切；`baseline-screen.png` 是采集时的桌面快照。由于当前缓存无设备，detail/history 视图没有可进入的真实设备记录；这一限制会在最终验收报告中单独标记，不能被 mock 数据掩盖。
